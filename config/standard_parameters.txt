#This is the standard configuration file for the simulation parameters.
#The configuration format is <parameter name/key> = <value> or <parameter name/key> : <value> 
#Spaces are necessary and must follow this pattern in order for the values to be parsed correctly.
#Any line starting with # or empty will be skipped, comments are made with # are not accepted after values# but are not accepted on value lines, they must have a dedicated line.
#Values accepted are numbers only when integers, numbers with a fullstop (.) at the end when doubles and a 1 for true, or 0 for false when booleans.
#i.e. non_pred_boidnum = 500 (integer)
#     pred_boid_num = 0      (integer)
#     separation : 20.       (double)
#     allignment : 3.        (double)
#     cohesion = 5.          (double)
#     detection_rad = 55.    (double)
#     danger_rad : 23.       (double)
#     angle_of_view = 360.   (double)
#     toroidal = false       (boolean)
#     op_rad : false         (boolean)
#Change this file to change the simulation parameters or change the path in the main to use a custom parameters file, some are provided in the configuration folder

non_pred_boidnum = 250
pred_boidnum = 1
separation = 20.
allignment = 3.
cohesion = 5.
detection_rad = 55.
danger_rad = 23.
angle_of_view = 360.
toroidal : true
op_rad : false

